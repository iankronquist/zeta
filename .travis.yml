sudo: false
language: c

compiler:
  - gcc
  - clang
script:
  - if [[ $CC == gcc ]]; then CC=gcc-5; fi;
  - if [[ $CC == clang ]]; then CC=clang-35; fi;
  - cd source
  - make
  - ./zeta --test
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - gcc-5
    - clang-35
